function Hero() {
  return (
    <section className="hero" aria-label="Hero">
      <h1 className="hero-title">
        Mint <span className="accent-mark">RFC 4122</span> identifiers
        <br />
        <span className="hero-title-2">at the speed of a keypress.</span>
      </h1>
      <p className="hero-sub">
        High-entropy UUIDs across v4 random, v1 timestamped, and v7 sortable.
        Formatted, batched, copied or downloaded without leaving the keyboard.
      </p>
    </section>
  );
}

export default Hero;
